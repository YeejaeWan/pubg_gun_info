import 'package:flutter/material.dart';

class DamageCalcScreen extends StatelessWidget {
  const DamageCalcScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('test'),
    );
  }
}
